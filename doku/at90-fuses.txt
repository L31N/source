Fuses müssen sein:

lfuse : 0xFF
hfuse : 0x99
efuse : 0xFF

you can fuse the lfuse using avrdude:
	$ avrdude -p c128 -c usbasp -U lfuse:w:0xFF:m

hfuse und efuse sind ab werk schon richtig eingestellt
